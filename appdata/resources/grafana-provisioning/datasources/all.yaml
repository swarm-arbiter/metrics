apiVersion: 1

datasources:
  - access     : 'proxy'
    editable   : true
    is_default : true
    name       : 'prometheus'
    type       : 'prometheus'
    url        : 'http://prometheus:9090'
    version    : 1
